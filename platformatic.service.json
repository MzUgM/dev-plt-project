{
  "$schema": "https://platformatic.dev/schemas/v0.23.1/service",
  "server": {
    "hostname": "{PLT_SERVER_HOSTNAME}",
    "port": "{PLT_PORT}",
    "logger": {
      "level": "{PLT_SERVER_LOGGER_LEVEL}"
    }
  },
  "plugins": {
    "paths": [
      "./plugins",
      "./routes"
    ]
  }
}
